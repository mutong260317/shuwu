# 数物 · 个人物品资产管理（自用 PWA）

长期主义 · 理智消费。记录物品、自动算日均/次均成本、过期提醒全家桶。
数据只存在浏览器本地（IndexedDB），不上传任何服务器。

## 一、启动服务（电脑端）

1. 双击 `启动服务.bat`（需已安装 Node.js；本机已装 v22）。
2. 窗口会显示两个地址：
   - 本机访问：`http://localhost:8787`
   - 手机访问：`http://192.168.x.x:8787`（以窗口显示为准）
3. 保持窗口打开。手机和电脑连**同一个 WiFi**。

## 二、添加到手机桌面（一套代码，双机通用）

### 小米 / 安卓
- 用 Chrome 或小米浏览器打开手机访问地址；
- 菜单 →「添加到桌面」/「发送至桌面」；
- 桌面出现「数物」图标，点开即用。

### iPhone
- 用 **Safari** 打开手机访问地址；
- 点分享按钮（方框+向上箭头）→「添加到主屏幕」；
- 桌面出现图标，打开后全屏独立运行，像原生 APP。

> 说明：局域网 HTTP 下是"桌面快捷方式"形态，功能完全一样；
> 若以后改用 HTTPS 部署，会自动升级为完整 PWA（离线缓存等）。

## 三、功能速览

- 总览：资产总值 / 综合日均成本 / 服役与退役数量 / 临期提醒预览
- 物品：搜索（买前查家里有没有）、分类筛选、日均成本排序
- 添加：名称/价格/日期必填，其余可选；保质期三种模式（固定到期日 / 生产+天数 / 开封+月份）、保修、订阅续费、愿望清单冷静期
- 详情：日均成本、次均成本、使用打卡、退役/恢复
- 提醒：保质期/保修/订阅/冷静期四类，红=过期、橙=临期、绿=安全；可"已处理"归档、订阅一键续期、冷静期结束"已买入/拔草"
- 统计：本月投入、分类 TOP、近 6 个月趋势、累计打卡
- 设置：临期阈值/冷静期天数可调；导出 JSON / CSV 备份；从 JSON 恢复

## 四、备份（重要）

数据在浏览器本地。以下情况前先「设置 → 导出 JSON」：
- 换手机 / 换浏览器；
- 路由器换了 IP（访问地址变了等于换了存储位置）；
- iPhone 长期不用可能清理离线数据。

到新环境打开应用 →「设置 → 从 JSON 恢复」即可。

## 五、目录结构

```
app/
├── index.html          应用入口
├── manifest.webmanifest PWA 清单
├── sw.js               Service Worker（HTTPS 下启用离线缓存）
├── server.js           本地静态服务（Node，端口 8787）
├── 启动服务.bat         一键启动
├── css/style.css       样式（有数风格：黑卡+荧光绿）
├── js/db.js            IndexedDB 存储层（含 localStorage 降级）
├── js/app.js           业务逻辑与界面
└── icons/icon-512.png  应用图标
```
