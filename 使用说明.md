# 数物 · 个人物品资产管理（自用 PWA）

长期主义 · 理智消费。记录物品、自动算日均/次均成本、过期提醒全家桶。
数据只存在手机浏览器本地（IndexedDB），不上传任何服务器。

## 一、线上地址（随时随地可用，推荐）

应用已部署到 GitHub Pages（HTTPS）：

> **https://mutong260317.github.io/shuwu/**

- 不需要开电脑、不需要同一 WiFi，手机有网就能打开；
- 首次打开后会自动缓存，之后**断网也能用**（离线 PWA）；
- 代码仓库：https://github.com/mutong260317/shuwu

## 二、添加到手机桌面（一套代码，双机通用）

### 小米 / 安卓
- 用 Chrome 打开线上地址；
- 菜单（右上角 ⋮）→「添加到主屏幕」/「安装应用」；
- 桌面出现「数物」图标，点开即用。

### iPhone
- 用 **Safari** 打开线上地址（必须 Safari，Chrome 不行）；
- 点底部分享按钮（方框+向上箭头）→「添加到主屏幕」；
- 桌面出现图标，打开后全屏独立运行，像原生 APP。

> 两台手机各自添加即可，应用是同一个；但**数据各自独立**（见备份章节）。

## 三、应用更新流程

以后要改功能/修 bug 时：

1. 在电脑上修改 `d:\ai\trae\记账\app` 里的文件；
2. 推送更新（在 app 目录执行，或让 AI 代劳）：
   ```
   git add .
   git commit -m "更新说明"
   git push
   ```
3. GitHub Pages 约 1 分钟后自动更新，手机上重新打开即是新版；
4. 若手机界面没变：彻底关闭 App 重开，或下拉/等待 Service Worker 更新缓存。

## 四、功能速览

- 总览：资产总值 / 综合日均成本 / 服役与退役数量 / 临期提醒预览
- 物品：搜索（买前查家里有没有）、分类筛选、日均成本排序
- 添加：名称/价格/日期必填，其余可选；保质期三种模式（固定到期日 / 生产+天数 / 开封+月份）、保修、订阅续费、愿望清单冷静期
- 详情：日均成本、次均成本、使用打卡、退役/恢复
- 提醒：保质期/保修/订阅/冷静期四类，红=过期、橙=临期、绿=安全；可"已处理"归档、订阅一键续期、冷静期结束"已买入/拔草"
- 统计：本月投入、分类 TOP、近 6 个月趋势、累计打卡
- 设置：临期阈值/冷静期天数可调；导出 JSON / CSV 备份；从 JSON 恢复

## 五、备份（重要）

数据存在**每台设备各自的浏览器**里，两台手机的数据不互通。以下情况前先「设置 → 导出 JSON」：
- 换手机 / 换浏览器；
- 清除浏览器数据 / 卸载重装桌面图标；
- iPhone 长期不用可能清理离线数据。

到新环境打开应用 →「设置 → 从 JSON 恢复」即可。

## 六、本地调试（可选，一般用不到）

仅当想在没有网络或改代码即时预览时使用：

1. 双击 `启动服务.bat`（需已安装 Node.js；本机已装 v22）；
2. 本机访问 `http://localhost:8787`，手机需与电脑同一 WiFi 访问窗口显示的 `http://192.168.x.x:8787`；
3. 注意：本地地址和线上地址是**两个独立存储空间**，数据不互通。

## 七、目录结构

```
app/
├── index.html          应用入口
├── manifest.webmanifest PWA 清单
├── sw.js               Service Worker（离线缓存）
├── server.js           本地静态服务（Node，端口 8787，可选）
├── 启动服务.bat         一键启动本地服务（可选）
├── css/style.css       样式（有数风格：黑卡+荧光绿）
├── js/db.js            IndexedDB 存储层（含 localStorage 降级）
├── js/app.js           业务逻辑与界面
└── icons/icon-512.png  应用图标
```
